<%= form_with(model: [@todo, @meta], url: todo_meta_path(@todo, @meta), local: true) do |form| %>
    <%= form.text_field :nome %>
    <%= form.hidden_field :completed, value: false %>
    <%= form.submit "Salvar mudanças" %>
<% end %>

<%= link_to 'Voltar', todo_metas_path(@todo, @metas) %>
  